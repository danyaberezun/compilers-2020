-- Expression evaluator

import List;
import State;



public fun evalExpr (st, expr) {
	case expr of
		  Var (x)	-> st(x)
		| Binop (op, l, r) -> binop(op, evalExpr(st, l), evalExpr(st, r))
		| Const (n)	-> n

	esac
}
